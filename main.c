
#include <curses.h>
#include "headers/all.h"


void loadMap(Map* map);

int main() {
    printf("Bienvenue dans Malloc World !\n") ;  
    //Game* game = initGame();
    //startGame(game);
    //freeGame(game);
    return 0;
}
